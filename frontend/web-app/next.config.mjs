/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'cdn.pixabay.com',
            'conteudo.imguol.com.br'
        ]
    }
};

export default nextConfig;
